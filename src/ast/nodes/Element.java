package ast.nodes;

/**
 * this class represents the blueprint of any element that could exist in the body of:
 * pages, controllers,in elements (with body), out elements (with body) or any basic node.
 * extend this class for the previously mentioned node classes
 */
public abstract class Element extends AbstractNode{

}
